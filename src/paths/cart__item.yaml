# /src/paths/cart__item.yaml

# /cart/{identifier}/item/{item_id}:
# Update item in cart
put:
  tags:
    - cart
  summary: Update line item in cart
  operationId: updateItemInCart
  parameters:
    - $ref: '../parameters/identifier.yaml'
    - name: item_id
      in: path
      description: id of the asset
      required: true
      schema:
        $ref: '../schemas/RecordId.yaml'
  requestBody:
    $ref: '../requestBodies/CartItem.yaml'
  responses:
    '200':
      description: successful update
      content:
        application/json:
          schema:
            $ref: '../schemas/Cart.yaml'
    '404':
      description: cart with provided id not found
    '405':
      $ref: '../responses/IllegalInput.yaml'
    '401':
      $ref: '../responses/UnauthorizedError.yaml'
  security:
    - bearerAuth: []
# Remove line item from cart
delete:
  tags:
    - cart
  summary: Remove line item from cart
  operationId: removeItemInCart
  parameters:
    - $ref: '../parameters/identifier.yaml'
    - name: item_id
      in: path
      description: id of the asset
      required: true
      schema:
        $ref: '../schemas/RecordId.yaml'
  responses:
    '200':
      description: successful update
      content:
        application/json:
          schema:
            $ref: '../schemas/Cart.yaml'
    '404':
      description: cart with provided id not found
    '405':
      $ref: '../responses/IllegalInput.yaml'
    '401':
      $ref: '../responses/UnauthorizedError.yaml'
  security:
   - bearerAuth: []