# /src/paths/carts.yaml

# /cart:
get:
  tags:
    - cart
  summary: Create a cart
  operationId: createCart
  responses:
    '201':
      description: Cart created
      content:
        application/json:
          schema:
            $ref: '../schemas/Cart.yaml'
  security:
    - bearerAuth: []
  