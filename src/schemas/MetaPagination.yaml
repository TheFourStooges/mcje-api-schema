# MetaPagination:
type: object
properties:
  total:
    type: integer
    format: uint64
  count:  
    type: integer
    format: uint64
  count_per_page:
    type: integer
    format: uint64
  current_page:
    type: integer
    format: uint64
  total_pages:
    type: integer
    format: uint64
  links:
    type: object