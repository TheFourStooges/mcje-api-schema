# MetaPagination:
type: object
properties:
  page:
    type: integer
    format: uint64
  limit:  
    type: integer
    format: uint64
  totalPages:
    type: integer
    format: uint64
  totalResults:
    type: integer
    format: uint64
  resultsThisPage:
    type: integer
    format: uint64
  links:
    type: object