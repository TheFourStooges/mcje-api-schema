# Category:
type: object
properties:
  id:
    $ref: './RecordId.yaml'
  parentId:
    $ref: './RecordId.yaml'
  slug:
    type: string
  name:
    type: string
  description:
    type: string
  isActive:
    type: boolean
    default: true
  createdAt:
    $ref: './UnixTimestamp.yaml'
  updatedAt:
    $ref: './UnixTimestamp.yaml'
  deletedAt:
    $ref: './UnixTimestamp.yaml'
xml:
  name: Category