# OrderPayment
type: object
properties:
  id:
    $ref: './RecordId.yaml'
  orderId:
    $ref: './RecordId.yaml'
  transactionId:
    type: string
  type:
    type: string
    enum:
    - "card"
    - "bank-transfer"
    - "cash-on-delivery"
    - "refund"
  amount:
    type: number
  currency:
    type: string
    minLength: 3
    maxLength: 3
  createdAt:
    $ref: './UnixTimestamp.yaml'
  updatedAt:
    $ref: './UnixTimestamp.yaml'
  deletedAt:
    $ref: './UnixTimestamp.yaml'
  