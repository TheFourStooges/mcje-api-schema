# CheckoutToken
content:
  application/json:
    schema:
      type: object
      properties:
        cartId:
          $ref: '../schemas/RecordId.yaml'
description: CheckoutToken request
required: true