# /src/requestBodies/LineItem.yaml

# LineItem:
content:
  application/json:
    schema:
      type: object
      properties:
        productId:
          $ref: '../schemas/RecordId.yaml'
        quantity:
          type: integer
description: Cart Item requestBody