# /src/requestBodies/LineItem.yaml

# LineItem:
content:
  "*/*":
    schema:
      type: object
      properties:
        product_id:
          $ref: '../schemas/RecordId.yaml'
        quantity:
          type: integer
        options:
          $ref: '../schemas/ProductOptionsDictionary.yaml'
        variant_id:
          $ref: '../schemas/RecordId.yaml'
      required:
        - product_id
        - quantity
description: Line Item requestBody
required: true