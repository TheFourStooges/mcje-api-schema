# Category:
content:
  application/json:
    schema:
      type: object
      properties:
        slug:
          type: string
        name:
          type: string
        description:
          type: string
        isActive:
          type: boolean
        # assets:
        #   type: array
        #   items:
        #     $ref: '../schemas/RecordId.yaml'
        parentId:
          $ref: '../schemas/IdUnion.yaml'
description: Category is a set of related products
required: true